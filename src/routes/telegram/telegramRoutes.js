const telegramController = require('../../controller/telegram/telegramController');
const router = require('express').Router();
const telegramHelper = require('../../helper/TelegramHelper');

// router.get('/', telegramHelper.executeTest);

// router.get('/html', telegramController.getHTML);

module.exports = router;
